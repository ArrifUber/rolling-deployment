events { }

http {
    upstream app {
        zone backend 64k;
        server app_rolling:5000 resolve;
    }
    resolver 127.0.0.11 valid=5s;

    server {
        listen 80;
        location / {
            proxy_pass http://app;
        }
    }
}